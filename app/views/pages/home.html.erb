<%= render 'shared/banner' %>

<div class="container" style="margin-top: 10px; margin-bottom: 20px">
  <br>

  <br>
  <h2 style= "text-align: center;">Get inspired by latest records on sale</h2>
  <br>

  <div class="cards">
    <% @vinyls.each do |vinyl| %>
      <%= link_to vinyl_path(vinyl) do %>
        <div class="card-full">
          <div class="card-full-infos">
            <div>
              <h2><%= vinyl.name %></h2>
              <p><%= vinyl.artist %></p>
            </div>
            <h2 class="card-full-pricing">£ <%= vinyl.price %></h2>
          </div>
            <%= cl_image_tag vinyl.photo %>
        </div>
      <% end %>
    <% end %>
  </div>
  <br>
  <div style= "text-align: center;">
    <%= link_to "Browse all", vinyls_path(@vinyl), class: 'btn btn-flat', style: 'color:white;'%>
  </div>
</div>

</div>
